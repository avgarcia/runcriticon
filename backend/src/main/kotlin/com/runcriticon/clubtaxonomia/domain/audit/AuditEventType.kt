package com.runcriticon.clubtaxonomia.domain.audit

/** Tipos de asiento de la auditoría local de `club_taxonomia`. Extensible: hoy solo cubre cambios de clasificación. */
enum class AuditEventType {
    TAGS_ALUMNO_ACTUALIZADOS,
}
