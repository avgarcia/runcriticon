<?xml version="1.0" encoding="UTF-8"?>
<!--
  Logging estructurado JSON (ADR-0011 D3). Salida a stdout → CloudWatch Logs (ADR-0006 D24).
  El MDC (module, club_id, user_id_hash, trace_id) se incluye automáticamente (ADR-0011 D5).
  En perfil local usamos salida legible por humanos; en el resto, JSON.
-->
<configuration>

    <springProfile name="local">
        <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
        <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <springProfile name="!local">
        <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeMdc>true</includeMdc>
                <fieldNames>
                    <timestamp>ts</timestamp>
                    <level>level</level>
                    <logger>logger</logger>
                    <thread>thread</thread>
                    <message>message</message>
                </fieldNames>
                <customFields>{"service":"runcriticon-app"}</customFields>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="JSON"/>
        </root>
    </springProfile>

</configuration>
