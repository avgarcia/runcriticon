# EditorConfig (https://editorconfig.org) — formato coherente entre IDEs
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 4

# Kotlin, Java
[*.{kt,kts,java}]
indent_size = 4
max_line_length = 120

# Tests en Kotlin: los @ArchTest/@Test usan nombres de propiedad entre acentos graves con espacios
# para describir reglas ArchUnit y casos (convención de Kotlin). property-naming no puede
# auto-corregirlos, así que se desactiva solo en las fuentes de test.
[**/src/test/**/*.kt]
ktlint_standard_property-naming = disabled

# TypeScript, JavaScript, HTML, CSS, SCSS
[*.{ts,js,html,css,scss,json}]
indent_size = 2
max_line_length = 120

# YAML, Terraform
[*.{yml,yaml,tf,tfvars}]
indent_size = 2

# Markdown
[*.md]
trim_trailing_whitespace = false
max_line_length = off

# Shell
[*.sh]
end_of_line = lf

# Batch
[*.{bat,cmd}]
end_of_line = crlf

# Makefile (tabs obligatorios)
[Makefile]
indent_style = tab
